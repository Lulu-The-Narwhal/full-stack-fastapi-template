---
description: "Unknown route patterns - use API routers, follow api/routers/ structure"
globs:
  - "api/**/*.py"
  - "**/routers/**/*.py"
  - "**/routes/**/*.py"
alwaysApply: false
---

# 🚀 API Route Patterns (Unknown)

## Router Structure

**Pattern**: `api/routers/{resource}.py`

**Example files from your codebase**:
- `frontend/src/routes/login.tsx`
- `frontend/src/routes/recover-password.tsx`
- `frontend/src/routes/_layout/settings.tsx`
- `frontend/src/routes/_layout/index.tsx`
- `frontend/src/routes/_layout/items.tsx`

## Real Example from Your Codebase

**Function**: `getItemsQueryOptions`  
**File**: `frontend/src/routes/_layout/items.tsx`  
**Usage Count**: 1

```python
function getItemsQueryOptions() {
    """Endpoint description."""
    # See `frontend/src/routes/_layout/items.tsx` for full implementation
```

## Route Definition Pattern

```python
# See existing routes in `api/routers/` for examples
# Check `frontend/src/routes/login.tsx` for the pattern
```

## Best Practices

1. **One router per resource** (e.g., `upload.py`, `jobs.py`)
2. **Use `API routers`** for route groups
3. **Follow RESTful patterns** (`/v1/{resource}/{action}`)
4. **See existing routes** in `api/routers/` for examples
5. **Use proper HTTP status codes** (201 for creation, 200 for success, etc.)

---

**When creating new API routes, follow the pattern in `frontend/src/routes/login.tsx`.**
