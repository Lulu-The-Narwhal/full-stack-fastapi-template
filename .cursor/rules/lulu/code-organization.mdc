---
description: "Project structure and code organization for full-stack-fastapi-template - unknown python"
alwaysApply: false
globs:
  - "**/*.py"
---

# 📂 Code Organization (unknown)

**Framework**: unknown
**Language**: python
**Generated**: 2026-02-05T15:30:48.180Z

**This project follows strict organization patterns. ALWAYS follow them.**

---

## Project Structure

`frontend/`    # 707 files - Contains: constant, function, type
`backend/`    # 165 files - Contains: function, class, constant

---

## 1. Constants

**Location**: `config/`
**Naming**: UPPER_SNAKE_CASE
**Pattern**: Group related constants together

**Examples from your project**:

```python
# undefined
DATABASE_URL = "postgresql://..."
API_VERSION = "v1"
MAX_RETRIES = 3
```


---

## 2. Utility Functions

**Location**: `frontend`
**Documentation**: Required (JSDoc/docstrings)
**Pattern**: Pure functions, testable, single responsibility

**Examples from your project**:

```python
# undefined

def findEmail(email: str) -> bool:
    """Validates email address format.
    
    Args:
        email: Email address to validate
    
    Returns:
        True if valid, False otherwise
    """
    return re.match(r'^[^@]+@[^@]+\.[^@]+

---

## 3. Types & Interfaces

**Location**: `frontend`
**Naming**: PascalCase for types/interfaces/models
**Pattern**: Colocate related types, export named

**Examples from your project**:

```python
# undefined
from pydantic import BaseModel
from enum import Enum

class UserRole(str, Enum):
    ADMIN = "admin"
    USER = "user"

class User(BaseModel):
    id: str
    email: str
    role: UserRole
```


---

## 4. Backend Modules

**Pattern**: Single responsibility, clear interfaces, well-documented
**Example API route**:

```python
# api/routers/users.py
from fastapi import APIRouter

router = APIRouter()

@router.post("/users")
async def create_user(user_data: UserCreate):
    # Business logic here
    return {"id": "123", "name": user_data.name}
```


---

## 5. Tests

**Location**: `tests/`
**Framework**: jest
**Pattern**: Mirror source structure, comprehensive coverage

**Example**:

```python
# tests/test_validation.py
from app.utils.validation import validate_email

def test_validate_email():
    assert validate_email("user@example.com") is True
    assert validate_email("invalid") is False
```


---

## 🚫 VIOLATIONS = QUALITY FAILURE

- Utils not in `frontend` → Quality score <80%
- Types not in `frontend` → Quality score <80%

**Quality must be ≥80% before proceeding!**

---

## 📋 Quick Reference

| Code Type | Location | Naming | Example |
|-----------|----------|--------|---------|
| Utils | `frontend` | camelCase | `validateEmail(email)` |
| Types | `frontend` | PascalCase | `interface User {...}` |
| API Routes | `frontend` | snake_case | `create_user_endpoint` |
| Services | `services/` | PascalCase | `UserService` |

---

**Generated by Lulu from .lulu/profile.json**
**Last scan**: 2026-02-05T15:30:48.180Z
, email) is not None
```


---

## 3. Types & Interfaces

**Location**: `frontend`
**Naming**: PascalCase for types/interfaces/models
**Pattern**: Colocate related types, export named

**Examples from your project**:

```python
# undefined
from pydantic import BaseModel
from enum import Enum

class UserRole(str, Enum):
    ADMIN = "admin"
    USER = "user"

class User(BaseModel):
    id: str
    email: str
    role: UserRole
```


---

## 4. Backend Modules

**Pattern**: Single responsibility, clear interfaces, well-documented
**Example API route**:

```python
# api/routers/users.py
from fastapi import APIRouter

router = APIRouter()

@router.post("/users")
async def create_user(user_data: UserCreate):
    # Business logic here
    return {"id": "123", "name": user_data.name}
```


---

## 5. Tests

**Location**: `tests/`
**Framework**: jest
**Pattern**: Mirror source structure, comprehensive coverage

**Example**:

```python
# tests/test_validation.py
from app.utils.validation import validate_email

def test_validate_email():
    assert validate_email("user@example.com") is True
    assert validate_email("invalid") is False
```


---

## 🚫 VIOLATIONS = QUALITY FAILURE

- Utils not in `frontend` → Quality score <80%
- Types not in `frontend` → Quality score <80%

**Quality must be ≥80% before proceeding!**

---

## 📋 Quick Reference

| Code Type | Location | Naming | Example |
|-----------|----------|--------|---------|
| Utils | `frontend` | camelCase | `validateEmail(email)` |
| Types | `frontend` | PascalCase | `interface User {...}` |
| API Routes | `frontend` | snake_case | `create_user_endpoint` |
| Services | `services/` | PascalCase | `UserService` |

---

**Generated by Lulu from .lulu/profile.json**
**Last scan**: 2026-02-05T15:30:48.180Z
